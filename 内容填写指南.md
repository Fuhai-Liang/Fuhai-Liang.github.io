# 博客内容填写指南（粘贴你的 38 篇文章）

你的每篇文章都对应一个 **Markdown 文件**，放在：

```
src/content/posts/
```

你只需要：**打开对应的 `.md` 文件 → 把 Markdown 正文粘贴进去 → 改一个开关**，文章就会自动出现在网站上，不需要碰任何代码。

---

## 一、一篇文章长什么样

打开 `src/content/posts/` 下的任意一个 `.md` 文件，顶部有一段用 `---` 包起来的信息（叫 frontmatter）：

```markdown
---
title: "文章标题"
description: "一句话摘要"
category: note          # 分类，见下方对照表
project: other          # 只有 category=project 时才需要
badge: "笔记"           # 首页卡片左上角的标签
tags: ["RAG", "Embedding"]
date: "2026-09-22"
pinned: false           # true = 置顶
draft: true             # 改 false 才会发布显示
---

这里开始就是正文，直接粘贴你的 Markdown。
```

### 粘贴步骤（三步）

1. **改标题和摘要**：把 `title` 和 `description` 改成你自己的。
2. **发布开关**：把 `draft: true` 改成 `draft: false`，文章就会显示出来。
3. **粘贴正文**：把 `---` 下面的内容，换成你写好的 Markdown 正文，直接粘贴即可。

> 小提示：正文里的小标题用 `##`（二级标题），不要再用 `#`（一级标题），因为页面标题已经由 frontmatter 里的 `title` 自动生成。

---

## 二、分类对照表（category 字段）

| category 值 | 显示在哪个栏目 | 首页卡片标签 |
| --- | --- | --- |
| `project` | 「项目」页 | 项目 |
| `experience` | 「经历」页 | 经历 |
| `video` | 「视频」页 | 视频 |
| `life` | 「生活随记」页 | 生活 |
| `note` | （仅首页，不单独建栏目） | 笔记 |

**改分类 = 改 `category:` 后面那一个单词**，文章就会自动跑到对应栏目，无需其它操作。

---

## 三、项目分组（project 字段，仅 project 分类需要）

在「项目」页里，文章会按 `project` 字段分组：

| project 值 | 分组名称 |
| --- | --- |
| `weather-agent` | 城市天气 Agent |
| `scga` | SCGA · 车辆重识别 |
| `rag` | RAG 学习与工程实践 |
| `other` | 其他项目 |

---

## 四、文件命名 = 网址

文件名（去掉 `.md`）就是网址。例如：

- `src/content/posts/weather-agent.md` → 网址 `/posts/weather-agent/`
- 想改名，直接重命名文件即可。

---

## 五、图片怎么放

把图片放进 `public/images/` 目录，然后在 Markdown 里这样引用：

```markdown
![图片说明](/images/你的图片名.jpg)
```

---

## 六、本地预览

```bash
npm run dev
```

然后浏览器打开终端里提示的地址（默认 `http://localhost:4321`），改动保存后页面会自动刷新。

正式发布用：

```bash
npm run build
```

---

## 七、当前文件说明（共 42 个）

| 文件 | 状态 |
| --- | --- |
| `welcome.md` | ✅ 已发布（欢迎帖） |
| `weather-agent.md` | ✅ 已发布（项目① 城市天气 Agent） |
| `scga.md` | ✅ 已发布（项目② SCGA · 车辆重识别） |
| `rag-notes.md` | ✅ 已发布（项目③ RAG 学习笔记） |
| 其余 38 个 `.md` | ⏳ 你的 CSDN 文章，已按标题/分类建好槽位 |

**38 篇 CSDN 文章 → 本地文件的一一对应关系，请看《文章对照表.md》。**

所有待填写文件都设了 `draft: true`，不会显示在网站上；填好内容、把 `draft` 改成 `false` 即上线。

---

## 八、开始粘贴

1. 打开《文章对照表.md》，找到你要迁移的那一篇。
2. 打开对应的本地 `.md` 文件，把 CSDN 上的正文复制成 Markdown 粘进去。
3. 把 `draft: true` 改成 `draft: false`，保存后 `npm run dev` 即可看到。

> 提示：CSDN 正文是网页（HTML），直接复制时代码块和图片可能需要整理。图片若是 CSDN 图床外链，通常能直接显示；若想本地化，把图片存到 `public/images/`，再在正文里用 `/images/xxx.png` 引用即可。
