# 博客上线部署说明（GitHub Pages + 自定义域名）

你的博客是 **Astro 静态网站**，构建后是纯 HTML/CSS/JS，可以免费托管在 GitHub Pages 上。

> 关键认知：**静态博客上线后是公开的，任何人有网址就能直接查看，访客不需要注册登录**。
> 「登录 / 评论留言」属于另一套后端功能（见文末说明）。

---

## 一、部署到 GitHub Pages（免费）

### 第 1 步：创建一个名为 `Fuhai-Liang.github.io` 的空仓库

1. 打开 https://github.com/new
2. Repository name 填：**`Fuhai-Liang.github.io`**（必须正好是你的用户名 + `.github.io`）
3. 选 **Public**，不要勾选任何初始化选项（README/gitignore 都不勾）
4. 点 **Create repository**

### 第 2 步：把本地代码推上去

创建好仓库后告诉我一声，我直接用 SSH 帮你推；或者你自己在终端执行：

```bash
cd ~/Desktop/fuhai-blog-v0.3
git remote add origin git@github.com:Fuhai-Liang/Fuhai-Liang.github.io.git
git push -u origin main
```

### 第 3 步：开启 Pages（Source 选 GitHub Actions）

1. 打开仓库页面 → **Settings** → 左侧 **Pages**
2. 在 **Build and deployment → Source** 下拉框里选 **GitHub Actions**
3. 保存

之后每次你把代码 `git push` 到 `main`，GitHub 会自动构建并部署。部署完成后，你的网址就是：

```
https://fuhai-liang.github.io/
```

---

## 二、绑定自己的域名（腾讯云 / 阿里云）

### 第 1 步：买域名
在 **腾讯云（DNSPod）** 或 **阿里云（万网）** 买一个域名，例如 `fuhai-blog.com`（或 `.cn / .top` 等更便宜的）。**买完需要实名认证**（国内域名规定）。

### 第 2 步：解析域名（DNS）
到你的域名控制台 → 域名解析，添加记录：

**情况 A：绑定根域名（如 `fuhai-blog.com`）** —— 添加 4 条 **A 记录**，主机记录填 `@`，记录值分别为：

```
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153
```

**情况 B：绑定 www 子域名（如 `www.fuhai-blog.com`）** —— 添加 1 条 **CNAME 记录**：

```
主机记录：www
记录类型：CNAME
记录值：fuhai-liang.github.io
```

（也可以两种都配：A 记录管根域名，CNAME 管 www。）

### 第 3 步：在博客里加 CNAME 文件
把 `public/CNAME` 文件内容写成一个域名（只能一个，建议 `www.fuhai-blog.com`）。你买好域名后告诉我，我帮你加好并改配置。

### 第 4 步：GitHub 设置自定义域名
仓库 **Settings → Pages → Custom domain** 填入你的域名，保存，勾选 **Enforce HTTPS**（等待证书生效，通常几分钟）。

---

## 三、关于「登录 / 评论」的说明

你之前提到"其他人也可以登录查看"。这里区分两件事：

| 功能 | 现状 | 说明 |
| --- | --- | --- |
| 访客查看网站 | ✅ 天然支持 | 上线后任何人打开网址就能看，无需登录 |
| 评论 / 留言 | ⏳ 占位 | 当前是 UI 原型，需要接后端（如 Giscus / Supabase） |
| 管理员登录后台 | ⏳ 占位 | 当前 `admin` 页是原型，未接真实账号系统 |

如果你需要**真实的评论留言**，最简单免费的方案是 **Giscus**（基于 GitHub Discussions，访客用 GitHub 账号评论）；需要**完整登录/后台**则用 **Supabase Auth + Database**。需要的话我可以帮你接。

---

## 四、日常更新流程

以后你改了文章（比如在 `src/content/posts/` 里粘了新文章），只需要：

```bash
cd ~/Desktop/fuhai-blog-v0.3
git add -A && git commit -m "更新文章" && git push
```

推送后 GitHub 自动重新部署，大约 1~2 分钟生效。
